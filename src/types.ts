export enum ColorSpaceType {
    HEX = "hex",
    CMYK = "cmyk",
    RGB = "rgba",
    HSL = "hsl",
    HSV = "hsv",
    OKLCH = "oklch",
    LCH = "lch",
  }
  
  export enum DisplayType {
    JSON = "json",
    TEXT = "text",
  }